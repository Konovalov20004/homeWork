//Это было интереснее, чем через три просто числа с тремя переменными, да и этот код подойдет для неограниченного количества чисел))

/*int lenghts = 3;
int[] numbers = new int[lenghts];
int desired = 0;

for (int count = 0; count < lenghts; count++)
{
    Console.WriteLine($"Введите число {count + 1}");
    int number = Convert.ToInt32(Console.ReadLine());
    numbers[count] = number;
};

foreach (int value in numbers)
{ 
    if ( value > desired)
    {
        desired = value;
    }
} Console.WriteLine($"Самое большое значение: {desired}");
*/